# 階段 A 進度與後續規劃

最後更新：2026-08-25。本檔記錄目前為止的介入結果、已排除的假說，以及
接下來的實驗順序。所有數字皆為 tw50、walk-forward、246 天 test。

> **2026-08-18：全部神經 arm 的數字已重算。** 先前的 metric 算在 MPS 上，
> 而 GATv2Conv 的 scatter-add 在 MPS 後端加總順序不固定——同一份 best.pt
> 連評 5 次 test IC 全距 0.0169（CPU 為 5 次位元相同）。權重與資料本身無損
> （save/load 位元正確、y 欄位對到 1e-16），壞的只有記錄 metric 時的 device。
> 下表已全部改為 CPU 重評值。機制、修復與限制見
> `docs/results_table.md` 的「Reproducibility defect and correction」。
>
> **重要限制：** 重評修正的是「報告的數字」，不是「選點」。best epoch 當初
> 是用 MPS 算的 val IC 選的，選點本身仍在同量級的噪音下決定。要完全乾淨
> 必須用 `eval_device: cpu` 重跑。相距 0.005 以內的排名一律視為暫定。

---

## 1. 目前的最佳 MAGNET 設定（但不是最佳神經模型）

> **2026-08-30：主結果 arm 換成 `tw50_betaF1nA2r1`**
> （= beta 層 + F1 只留 log_return + 關掉台股層圖 A₂ + rank 損失權重 1.0）。
>
> 換的理由與前幾次不同——這次有**獨立測試期**的證據：
>
> | | 第一折 | **第二折**（2023-12 ~ 2024-12，與第一折不重疊） |
> |---|---:|---:|
> | ΔIC（對前版） | +0.0079 | **+0.0308** |
> | ΔRankIC | +0.0131 | **+0.0262** |
> | 同號種子 | 10/10 | **10/10** |
> | **逐日 p** | 0.3214 / 0.0551 | **0.0004 / 0.0036** |
>
> **這是本專案第一次有架構改動在逐日層級對自家基準顯著，
> 且是在一個從未用來選模型的測試期上。** 判準見 proposal §36（事先登記）、
> 判定見 §38。
>
> **但不可寫「超越線性 baseline」**：第二折的 RankIC 輸給 KTW+（−0.0115）、
> 與 R2 ridge 恰好打平（+0.0000），IC 雖名目領先但無一顯著（§40）。
> 兩折合看相對最佳線性 baseline 是**打平**。
>
> 三個改動**單獨都無效甚至有害**（F1 +0.0030 ns、關 A₂ 的 IC −0.0050、
> rank 1.0 單獨 −0.0003），是超可加的交互作用，機制未確認（§35）。
>
> 前一版 `tw50_beta` 的設定如下；本版在其上加了三個旗標
> （`--features log_return`、`--graph-ablate empty_l2`、`--rank-weight 1.0`）。

```
architecture        magnet_weak_free
features            log_return, RSI_14, BB_pos      （feature_subset，3 維）
T_history           1
gat.num_layers      1          ※ config 的 num_heads=4 不生效：
                               　 num_layers=1 時最後一層恆為單頭
lstm.input_norm     batchnorm  （scope: shared）
raw_skip            無
training.optimizer  adamw
weak_links.beta_layer      true
weak_links.beta_init_std   0.3     alpha_j / gamma_j 的初始離散度
weak_links.beta_alpha_mean 1.0
weak_links.beta_gamma_mean 0.0
tag                 tw50_beta
參數量               50,570
```

耦合的形式：

```
ĥ₁ⱼ = ① α_j·h₁_p(j)·[paired] + ② γ_j·h̄₁ + ③ Σᵢ B[i,j]·(h₁ᵢ − h̄₁)
      7 檔的 ADR 特異訊號          50 檔的市場因子暴露   扣掉共同成分後的個股結構
```

### 1.1 這次換基準修好了上次的代價

上一次換基準（-> `tw50_inbnw_noskip`）的副作用是逐日檢定力下降，
其中最嚴重的是 **RC 常數對照的逐日 p 由 0.0171 升到 0.0580，跨過 0.05**。
`tw50_beta` 把它修回來了，而且對所有神經對照的逐日檢定都顯著：

| 對照 | 前版為基準時 | **`tw50_beta` 為基準時** |
|---|---|---|
| RC 常數對照 | +0.0369 / 逐日 p **0.0580** | +0.0862 / 逐日 p **0.0000** |
| Early fusion | −0.0001 / 0.9933 | **+0.0492 / 0.0047** |
| MAGNET-v2（前前版） | +0.0042 / 0.7284 | **+0.0536 / 0.0021** |
| R2（線性 baseline） | −0.0502 / 0.0033 | −0.0009 / 0.9374（**追平**） |

### 1.2 消融（重訓，各 10 顆種子）

| 拿掉 | Δ IC | 配對 t | 較優種子 |
|---|---:|---:|---:|
| ① ADR 特異訊號 | −0.0073 | 6.6e-03 | 1/10 |
| ③ 殘差聚合（1,493 條候選邊） | **−0.0179** | 2.0e-03 | 0/10 |
| ①+③（只留 ② 因子） | −0.0295 | 2.2e-03 | 0/10 |

**③ 的貢獻是 ① 的 2.5 倍。** 同一組候選邊在 beta 層之前
ΔIC 恰為 +0.0000（p=0.69）——先前它被迫搬運共同成分，
而該通道已被量級大 1,345 倍的恆等路徑佔滿。

② 單獨在架構裡達到 +0.0716，對照獨立線性 rank-1 beta 模型的 +0.0738，
差 −0.0022（該 arm sd 0.0223）——**架構本身不干擾 beta 機制**。

### 1.3 須揭露的退步

預測由收縮翻成**過度離散**：逐日橫截面 std(ŷ)/std(y) 由 0.45 變成 **1.60**，
MSE 0.00045 -> **0.00106**、R² **−1.4 ~ −2.8**。
IC / RankIC 是相關係數、不受尺度影響，所以排序的提升是真的；
但組合回測或任何報 MSE / R² 的地方都必須先處理這一項。

### 1.4 層內圖 A₁ / A₂ 的消融（2026-08-25 完成）

| 設定 | test IC | Δ vs 完整 | 逐日 Holm | 跨種子 Holm | 判定 |
|---|---:|---:|---:|---:|---|
| 完整 | +0.1011 | — | — | — | — |
| A₁ 只留 self-loop | +0.0985 | −0.0026 | 1.0000 | 0.1770 | 無結論 |
| A₂ 只留 self-loop | +0.0961 | −0.0050 | 1.0000 | 0.0961 | 無結論 |
| 兩層都只留 self-loop | +0.0956 | **−0.0054** | 1.0000 | **0.0382** | 僅跨種子顯著 |
| 兩層都隨機化 | +0.0776 | **−0.0235** | **0.0428** | **0.0000** | **兩者皆顯著** |

**層內圖的淨貢獻只有 +0.0054（佔總量 5%），且僅跨種子顯著。**
但把邊隨機化（邊數與邊權分布不變、只打亂端點）會掉 −0.0181，
逐日與跨種子雙雙顯著——**錯誤的鄰居會主動傷害**。

正確措辭：**相關係數圖顯著優於隨機圖，但只略優於完全不用圖。**
不可宣稱「層內圖結構是必要的」。跨層耦合的重要性是層內圖的 **5.5 倍**
（+0.0295 vs +0.0054）。完整判讀見 `docs/proposal_819_revision.md` §28。

---|---|---|
| MAGNET F3（無跳接） | +0.0234 / **0.0008** / 0.0026 | +0.0277 / 0.0760 / **0.0000** |
| MAGNET F9 T=1 L=1 | +0.0216 / **0.0052** / 0.0042 | +0.0258 / 0.1105 / **0.0000** |
| 稠密耦合 A | +0.0288 / **0.0016** / 0.0484 | +0.0330 / 0.0620 / **0.0002** |
| RC 常數對照 | +0.0326 / **0.0171** / — | +0.0369 / 0.0580 / — |

**效果量全部變大，跨種子 p 全部變強，但逐日 p 全部變弱。**
原因是 `r_day`：舊基準與這些對照是「同一個訓練設定 ± 一條路徑」，
逐日 IC 序列相關 0.80，配對能消掉大部分噪音；新基準同時改了輸入正規化
與優化器，`r_day` 掉下來，配對就幫不上忙。這是 `paired_daily.py`
說明文字裡已寫明的情況。

**須主動揭露：RC 常數對照的逐日 p 由 0.0171 升到 0.0580，跨過 0.05。**
RC 沒有種子，逐日是唯一可用的檢定，所以這一列現在不顯著。
兩個版本其實都不算舒服（0.017 也只是勉強），這是既有的弱點被放大，
不是換基準造成的新缺陷——但口試若被問到必須照實回答。

| arm | n | val IC | test IC (sd) | RankIC (sd) |
|---|---:|---:|---:|---:|
| [24] / KTW+ 等線性高原 | — | — | +0.098 ~ +0.108 | +0.087 ~ +0.110 |
| **Early fusion（T=20, F=9, L=2）** | 10 | +0.1013 | **+0.0518 (0.0049)** | +0.0412 (0.0040) |
| **F3 + BN 拼接跳接（本工作最佳）** | 10 | +0.0653 | +0.0475 (0.0180) | **+0.0513 (0.0129)** |
| MAGNET 原本（T1F9） | 10 | +0.0326 | +0.0259 (0.0059) | +0.0407 (0.0040) |
| F3 三特徵 | 10 | +0.0313 | +0.0241 (0.0090) | +0.0342 (0.0071) |
| RC 常數對照 | — | — | +0.0149 | +0.0317 |
| Early fusion（T=1, F=3, L=1） | 10 | +0.0304 | −0.0076 (0.0034) | +0.0006 (0.0083) |

跳接 vs F3 基準（同 10 顆種子）：

| 指標 | 差 | 95% CI | Welch | 同種子配對 t | Mann-Whitney | Wilcoxon | 逐日 Holm | 較優種子 |
|---|---:|---|---:|---:|---:|---:|---:|---:|
| IC | +0.0234 | [+0.0070, +0.0399] | 0.0030 | 0.0100 | 0.0060 | 0.0200 | 0.0049 | 8/10 |
| RankIC | +0.0172 | [+0.0042, +0.0301] | 0.0020 | 0.0146 | 0.0060 | 0.0371 | 0.0001 | 7/10 |

本專案第一個逐日與跨種子雙雙通過 Holm、且無母數檢定也顯著的架構改善。
重評後這個結論**變強**（IC 差 +0.0191 -> +0.0234，CI 下界 +0.0030 -> +0.0070，
Wilcoxon 0.049 -> 0.020），是全表唯一被修正推高的比較。
須揭露：BN arm 的種子變異（0.0180）仍明顯高於基準（0.0090）、
單一種子不保證較優。

### 但 MAGNET-v2 不是最佳神經模型（2026-08-17 更新）

Early fusion（輸入層直接拼接配對 ADR 特徵，無跨層耦合、無 gate）補到
10 顆種子後：

| 指標 | EF | MAGNET-v2 | 差 (v2−EF) | 95% CI | Welch | 配對 t | Mann-Whitney | Wilcoxon | v2 勝 |
|---|---:|---:|---:|---|---:|---:|---:|---:|---:|
| IC | +0.0518 | +0.0475 | −0.0043 | [−0.0177, +0.0090] | 0.477 | 0.481 | 0.678 | 0.492 | 4/10 |
| RankIC | +0.0412 | +0.0513 | +0.0101 | [+0.0011, +0.0191] | 0.038 | 0.032 | 0.104 | 0.049 | 7/10 |

**分歧判決。** IC 上 EF 仍較高且贏 6/10 顆（不顯著，p=0.48）；RankIC 上 v2 較高，
兩個母數檢定與 Wilcoxon 過 5%（0.049 恰在邊界），但 Mann-Whitney 重評後
**從 0.064 退到 0.104**。RankIC 這一項修正後是變弱而非變強，且各檢定彼此不一致。

公平性已處理：EF 在 MAGNET-v2 的輸入設定（T=1, F=3, L=1）下崩到 −0.0076
（對其原設定 p<0.001），故兩者各以自身最佳設定比較。

**目前可以宣稱與不可以宣稱：**

| 可以說 | 依據 |
|---|---|
| 跳接顯著改善 MAGNET | +0.0234，四種檢定全過，8/10 種子（修正後變強） |
| MAGNET-v2 顯著勝過六個文獻 baseline 與原始 MAGNET | +0.0414, p=0.048 |
| MAGNET-v2 在 RankIC 上優於 early fusion | **修正後更弱**：MW p=0.104，Wilcoxon 0.049，不宜再當作可宣稱項 |

| 不可以說 | 為什麼 |
|---|---|
| MAGNET 的多重圖耦合與 gate 是必要的 | 無圖耦合的輸入層拼接在 IC 上追平甚至更高 |
| MAGNET-v2 是最佳神經模型 | EF 的 IC 點估計較高，且贏 6/10 顆種子 |
| 任何相距 0.005 以內的排名 | 選點仍在 MPS 噪音下決定，須以 eval_device=cpu 重跑後才算數 |

---

## 2. 診斷主線：資訊在哪裡流失

以 per-target ridge 量各位置可線性抽取的 test IC（協定同 ridge_ladder）：

```
原始 3 特徵（3 x 30 = 90 維）                      +0.0874
耦合點 h_L1（基準）                                +0.0448   <- 編碼器丟掉 49%
耦合點 h_L1（BN 拼接，訓練後）                       +0.0693 ~ +0.0754
模型實際輸出（BN 拼接）                              +0.0475
```

兩個 gap：
- **Gap A** 編碼器（LSTM -> GAT_L1 -> proj_L1）丟掉約一半 —— BN 拼接修回大部分
- **Gap B** 模型只用掉耦合點的 60~71% —— 尚未動過

---

## 3. 已排除的假說（不要重做）

| 假說 | 結論 | 依據 |
|---|---|---|
| 表示塌縮（cos=1.0）是缺陷，拆開它會變好 | **推翻** | cs_demean 去共同成分 IC +0.0269 -> -0.0021；共同成分佔 h_L1 能量 99.97% |
| 可達性 7/50 是瓶頸 | **推翻** | dense A 做到 50/50，無改善；模型在無約束下把稠密邊縮到初始值的 13% |
| RSI_14 / BB_pos 恆為正造成共線，是負擔 | **推翻** | 拿掉後 IC 掉 45%，合併檢定 Mann-Whitney p=0.0020。它們是唯二的有界水準型特徵 |
| 特徵愈少愈好（線性階梯的排序會轉移） | **推翻** | 1/2/4/7 維彼此無差異（ANOVA p=0.9622），關鍵是有沒有 RSI+BB |
| 加法式跳接能把 raw 送進耦合點 | **推翻** | 耦合點 +0.0448 -> +0.0446。相加把 raw 壓進已被佔滿的 d' 維，不可分 |
| 多重圖耦合與 gate 是跨市場預測的必要結構 | **未獲支持** | Early fusion（輸入層拼接，無耦合無 gate）IC +0.0518 vs v2 +0.0475，贏 6/10 顆種子 |
| baseline 沒調過才輸給我們 | **推翻** | EF 在 v2 的調校設定下崩到 −0.0076；兩者需要不同輸入 regime，各以最佳設定比較後 EF 仍較高 |

### 兩個實作層的教訓

1. **上限測試必須用模型實際會用的算子鏈。** 曾把「拼接」的上限（+0.0662）
   誤當成「相加」的，導致 2a 白跑一輪。
2. **LayerNorm 不可用於特徵軸的正規化。** F=3 時它把 3 個數字壓到剩 1 個
   自由度，跳接路徑 raw +0.0874 -> **+0.0311**（掉 64%）。2a 與 2a' 兩次實驗
   因此都不算數。正確算子是逐特徵跨節點（`_FeatureNorm`，BatchNorm 語意），
   線性探針量到 +0.0874，與未正規化的原始特徵**逐位相同**——逐特徵標準化
   是每個特徵一個仿射映射、對所有節點相同，線性模型本來就吸收得掉，
   所以它在資訊上零損失。`norm="none"` 資訊同樣無損但模型實測較差，
   原因是尺度差 5000 倍造成線性層條件數問題，屬最佳化而非資訊問題。
   （2026-08-17 重測，`scripts/ridge_ladder.py` 同協定；先前記為 +0.0332
   的數字有誤。）

---

## 4. 實驗紀律（本專案的判讀標準）

sigma_seed = 0.0062（IC，兩組各 10 顆種子獨立量到同一值）。

| 每組 n | MDE（80% 檢定力） | 無母數雙尾 p 下限 |
|---:|---:|---:|
| 3 | 0.0188 | 0.10（永遠無法顯著） |
| 5 | 0.0125 | 0.0079 |
| 10 | 0.0082 | 0.0002 |
| 20 | 0.0056 | — |

用專案自己的兩組 10 顆種子窮舉 14,400 個「只跑 3 顆」的平行實驗：

- 符號錯誤 10.4%（IC）
- 檢定力 5.8%，而自己比自己的偽陽性率 6.0% —— **兩者相同，n=3 對這個量級不產生證據**
- 顯著時效果量誇大 2.26x（實際在 BN 跳接上驗證為 1.41x）

**規則：3 顆探路只看方向；下結論前補到 10 顆；效果量 < 0.01 一律寫「無法區分」。**
`scripts/paired_daily.py` 會自動標記「無結論」。

---

## 5. 後續實驗順序（2026-08-26 全面改寫）

前一版寫於 2026-08-17，在輸入正規化、移除跳接、beta 層、層內圖消融之前。
**當時 14 個項目裡有 9 個的前提已被我們自己的實驗推翻**，
不是「做完了」而是「問題不存在了」。逐項交代見 §5.4。

判準沿用 §4：n=10 的 MDE 是 0.0082，逐日與跨種子兩個檢定要分開看。

---

### 5.1 A 級 —— 防守性，不做會在口試或審查被打

| # | 項目 | 成本 | 為什麼非做不可 |
|---|---|---|---|
| **A1** | **修過度離散**：掃 `loss_weights.variance`，或改 rank-only | 4 值 x 10 種子，約 4h | **這是 beta 層自己造出來的退步。** 橫截面 std(ŷ)/std(y) 由 0.45 翻到 **1.69**，MSE 0.00045 -> 0.00106、R² −1.4~−2.8。只要有人問「你的 MSE 呢」就是硬傷，而且組合回測在修好前完全不能做 |
| **A2** | **Early fusion + `input_norm`** | 10 種子，約 3h | 我們的 arm 拿了 +0.0219，baseline 沒拿。**這是「MAGNET 掃約 50 組設定、六個文獻 baseline 各跑一組」那個公平性缺口最容易被打的一格**，而且判準是我們自己在 `baseline_early_fusion.py` 的 docstring 立的 |
| **A3** | **單市場 baseline**：①②③ 三條路徑全關 | 10 種子，約 3h | 教授建議 1（單市場 baseline 用消融做）。也是真正的「無跨市場」對照——目前**不存在**這個 arm，`LSTM only` 是另一個架構，不對等 |
| **A4** | 把「舊式是不是有 bug」的兩個檢查寫成正式測試 | 約 1h | 見 §5.3。這個質疑一旦被問過，之後每次改架構都會再被問一次 |

A1–A3 都是訓練，可以連續排成一個批次，約 10 小時，一個晚上跑得完。

---

### 5.2 B 級 —— 推進論文主張

| # | 項目 | 成本 | 帶來什麼 |
|---|---|---|---|
| **B1** | **B 的產業聚類正式化**：腳本 + 圖 + 跨種子表 | 約 2h，不重訓 | **③ 目前唯一的可解釋性證據**。B 的欄位在完全沒有產業標籤下自己分出電子 / 金融 / 防禦，10/10 種子成立（分離度平均 +0.4494）。目前只存在於 scratchpad |
| **B2** | **y 拆成隔夜跳空 + 盤中**（原 4.1） | 純分析，約 2h | 從「次要分析」升級成**機制證據**：beta 層明確在模型化因子暴露，若那個暴露主要經隔夜跳空傳導，就把「跨市場 = 市場因子」由相關性推進到傳導管道，而且不必重訓 |
| **B3** | **`beta_init_std` 敏感度** | 5 值 x 3 種子，約 4h | 0.3 是有理據的猜測，**從未掃過**。審查者一定會問「這個超參多敏感」 |

---

### 5.3 為什麼要有 A4：重參數化質疑

教授已提出：`②+③` 與舊式 `Σᵢ B_eff[i,j]·h₁ᵢ` 在數學上等價，
那舊式學不到是不是程式有錯？完整回應見
`proposal_819_revision.md` §33。兩個檢查應固定成測試：

| 檢查 | 目前結果 | 要固定的斷言 |
|---|---|---|
| 舊式的 `weak_beta` 在初始化時有梯度 | 平均 \|grad\| **6.9e-04**，全模型中位數 3.6e-04 | B 的梯度不得為零，且不得低於全模型中位數一個數量級 |
| 新模型換算成 B′ 後，用舊式函數形式重算 ĥ₁ | 43 檔無配對 max\|diff\| **4.8e-07** | 對無配對節點必須逐位相同（新增的是最佳化幾何，不是表達能力） |

---

### 5.4 C 級與已刪除項目

**C 級——仍有效，但期望值低於 A/B**

| # | 項目 | 現況 |
|---|---|---|
| C1 | 多因子（rank-3，原 §24.3 的 P6） | **目標已被越過**：+0.1011 > rank-3 的 +0.0887。剩餘空間是到線性高原的 +0.0009~+0.0066，**低於 MDE 0.0082** |
| C2 | EF 的 val/test 落差診斷（原 0.1） | 仍有效未做。但 EF 已由「贏我們 0.0043」變成「輸 0.0492」，威脅性下降 |
| C3 | rank loss 的 `rank_normalize` 重測（原 2.3） | 仍有效未做 |
| C4 | 外生變數 / 擴充 universe（原 4.2） | **這是唯一能抬高天花板的方向**——探針顯示輸入（+0.0874）已低於 30 維 ridge 的 +0.1020，架構改不動這一格。但**動到資料層，需要逐次明確授權** |

**已刪除——前提被自己的實驗推翻，重測沒有資訊價值**

| 原項目 | 為什麼死了 |
|---|---|
| 0.3 EF + raw skip | **跳接已從架構移除**。正規化修好後其增量由 +0.0234 掉到 +0.0060（皆不顯著） |
| 1.1 跳接拉回 F9 | 同上 |
| 1.2 `bnboth` 補到 10 顆 | 同上 |
| 1.3 重測 dense A | 訊號 72% 是 rank-1 市場因子，「更好的兩兩對應」問錯了問題 |
| 1.4 重測 `cs_demean` | **已被 beta 層的結構吸收**：③ 本來就只乘 `h₁ᵢ − h̄₁` |
| 2.1 gate 診斷與改造 | **前提消失**。gate 逐維範圍已由 0.483–0.551 變成 **0.062–0.980**，跨節點 sd 由 0.00046 變成 0.005579（12x）。它不再是常數 |
| 2.2 預測頭加寬 | 剩餘空間 < MDE；且 §26.2 直接驗證共享讀出對 beta 機制已足夠（只有 ② 拿到 +0.0716 vs 獨立線性 +0.0738） |
| 3.1 耦合直接用 raw | 編碼器那一格只剩 −1%，後來甚至變成 +14% |
| 3.2 低秩 A（壓種子變異） | AdamW 已達成（sd 0.0180 -> 0.0021） |
| §24.3 的 P2 通道分離 + concat | gate 前提消失（同 2.1）；且 §11.4 量過 concat +0.0909 vs gated +0.0880，val 反而較低 |
| §24.3 的 P5 耦合輸出去均值 | 同 1.4，已被結構吸收 |
| 動態 B / cross-attention | 訊號只有 1~3 維，容量從來不是綁住的那一環 |

---

## 6. 論文層面的待辦

- **更正（2026-08-26）：「43/50 節點跨市場輸入位元為零」這句話是錯的，
  不可再用。** 舊架構有候選邊，那 43 檔拿到的是**極小但非零**的值。
  實測隱含敏感度（`[配對] + colsum_j(B)`，2025-04-10、seed 42）：

  | arm | 配對 7 檔 | 無配對 43 檔 | 倍數 |
  |---|---:|---:|---:|
  | MAGNET-v2 | +1.0044 | +0.0139（sd 0.0923） | 72x |
  | 輸入正規化（前主結果） | +1.0002 | +0.0001（sd 0.0008） | 12,295x |

  正確的說法是「架構給了這個自由度，但梯度下降學不起來」——
  這個區別很重要，因為前者代表「缺一條路，要補模組」（補破洞），
  後者代表「路被塞住，要找出為什麼」（從理論下手）。詳見
  `proposal_819_revision.md` §33。
- **不依賴種子的確定性證據**是主證據，應優先成圖：
  h_L1 共同成分佔能量 **99.78%**（舊 arm；beta arm 降到 92.86%）、
  拿掉候選邊 ΔIC 恰為 +0.0000（p=0.69）、模型主動把稠密邊縮到 13%、
  RSI_14 的 std 是 log_return 的 471 倍。
  （原記的 99.97% 與「lambda=1e-3 只送出 0.08%」皆已過期：
  前者是不同 arm 的量測，後者的 lambda 現在是 0。）
- 簡報 p18 的 GELU/ReLU 與損失權重 1:0.5（實際梯度 1:3）須更正。
- `build_model` 把 `weak_links.mode` / `coupling.mode` 注入局部 cfg，
  `config_snapshot.yaml` 沒記錄到 —— 重現性缺口，應補。
- **MPS 非確定性（2026-08-18 已修）。** 2026-08-18 之前的 metric 全部算在
  MPS 上，同一份 best.pt 重評的 test IC 全距 0.0169，與跨 seed sd 同量級。
  已改為 `eval_device: cpu` + 每個 run 自我驗證 + 3 個迴歸測試。
  論文須揭露：現有數字是「對已選定 checkpoint 的正確重評」，但**選點**
  當初仍是用 MPS 的 val IC 決定的。要主張任何相距 0.005 以內的排名，
  必須先用 `eval_device: cpu` 從頭重跑該 arm。
  另：`mlflow.source.git.commit` 記的是 d0ecf76，但 2026-08-16 那批 run
  實際跑的是尚未 commit 的工作區程式碼（後來成為 b0f4fbd）——
  run 與 commit 的對應不可信，應在跑實驗前先 commit。
- 六個文獻 baseline 各只跑一組預設超參，MAGNET 跑了約 50 組。
  這是最大的公平性缺口，審查者會先問。Early fusion 已補到 10 顆種子
  並加測同設定版本，其餘五個尚未。
- **論述須改寫（2026-08-26 更新）。** 原文寫「目前證據只支持 raw skip 這個
  元件有效」——**跳接已移除**，該句作廢。現在的證據支持這三句，且各有消融：

  | 可以主張 | 依據 |
  |---|---|
  | 跨層耦合有貢獻：三條路徑合計 +0.0295，其中 ③ 的 +0.0179 是 ① 的 2.5 倍 | §1.2 |
  | 層內相關係數圖顯著優於隨機圖（+0.0181，雙重顯著），但只略優於完全不用圖（+0.0054，僅跨種子顯著）——**兩句必須一起講** | §1.4 |
  | 貢獻是「診斷 + 重參數化」，不是「新的耦合機制」 | proposal §33 |

  **仍不可寫**「多層圖架構有效」——得到驗證的是跨層的三條路徑，
  層內圖只佔 5%。也不可寫「我們超越線性 baseline」——是追平。
